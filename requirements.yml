---
# Ansible Galaxy collections required by all playbooks in this repo
collections:
  - name: community.proxmox
    version: ">=1.4.0"
  - name: community.general
    version: ">=9.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: ansible.utils
    version: ">=4.0.0"
  - name: ansible.netcommon
    version: ">=6.0.0"
